apiVersion: v1
kind: Service
metadata:
  name: reservation-service
spec:
  type: NodePort
  selector:
    app: reservation-service
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30000
